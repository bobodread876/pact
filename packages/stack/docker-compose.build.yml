# Override to build pactd from source instead of pulling the published image.
# Usage:
#   docker compose -f docker-compose.yml -f docker-compose.build.yml up -d --build
services:
  pactd:
    build:
      context: ../..
      dockerfile: packages/stack/Dockerfile
    image: pactd:local
