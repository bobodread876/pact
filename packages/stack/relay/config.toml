# Minimal nostr-rs-relay config for the bundled pact-stack relay.
[info]
name = "pact-stack relay"
description = "Local Nostr relay for Pact bonds (NIP-BD kinds 30317/1317)."

[database]
data_directory = "/usr/src/app/db"

[network]
address = "0.0.0.0"
port = 8080

[limits]
messages_per_sec = 0
