{
  "name": "pact-cli",
  "version": "0.5.0",
  "description": "Pact CLI — form, resolve, and verify agent bonds from the shell.",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "pact": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc"
  },
  "dependencies": {
    "pact-core": "^0.5.0",
    "pact-sdk": "^0.5.0"
  }
}
