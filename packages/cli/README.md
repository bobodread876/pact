# @pact/cli *(stub)*

The `pact` CLI — a higher-level superset of the `mate` CLI for ops, scripting, and sovereign users (identity, bonds, relays, self-host helpers).

Not yet implemented — see [ARCHITECTURE.md §4](../../ARCHITECTURE.md).
