# @pact/sdk *(stub)*

TypeScript SDK to embed bonds **in-process** in an agent. Will wrap `@pact/core` with an ergonomic surface (`bond.propose/accept/reaffirm/revoke`, `onStateChange`, consent policy). A Python `pact-sdk` will be a thin client to `pactd`.

Not yet implemented — see [ARCHITECTURE.md §4](../../ARCHITECTURE.md).
